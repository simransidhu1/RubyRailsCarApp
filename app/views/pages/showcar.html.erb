<div class="min-h-screen bg-gradient-to-r from-green-400 to-blue-500 flex items-center justify-center p-6">
    <div class="w-full max-w-2xl bg-white bg-opacity-90 shadow-xl rounded-lg p-8 backdrop-blur-md">
        <h1 class="text-4xl font-extrabold text-center text-blue-800 mb-6">🚗 Car Details</h1>

        <div class="space-y-4">
            <h2 class="text-2xl font-bold text-gray-800"><%= @car.model %> - <%= @car.year %></h2>

            <p class="text-lg text-gray-700"><strong>🏢 Company:</strong> 
                <span class="text-blue-600 font-semibold"><%= @car.company.name %></span>
            </p>
            
            
        </div>

        <!-- Back Button -->
        <div class="mt-8 text-center">
            <%= link_to '🔙 Back to Cars', cars_path, class: "px-6 py-3 bg-blue-600 text-white font-semibold rounded-lg shadow-md transition duration-300 hover:bg-blue-700 transform hover:scale-105" %>
        </div>
    </div>
</div>
